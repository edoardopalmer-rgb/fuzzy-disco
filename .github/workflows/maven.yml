name: Java CI with Docker (Chrome fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Build Docker image
      run: docker build -t my-java-app .

    - name: Run tests inside Docker (with unique Chrome user data)
      run: |
        # Crea una dir temporanea da montare come user-data-dir
        mkdir -p ${{ runner.temp }}/chrome-user-data

        docker run --rm \
          -v ${{ github.workspace }}:/app \
          -v ${{ runner.temp }}/chrome-user-data:/tmp/chrome-user-data \
          -w /app \
          my-java-app
